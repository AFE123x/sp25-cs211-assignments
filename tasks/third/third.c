#include<stdio.h>

/**
  * In this part, you're going to implement a function that finds the
  * largest element in an array. James is very particular that you
  * shouldn't use indexing (using []). Instead, you should use pointer
  * arithmetic. If you use indexing, you'll not get the FT offer D-:
  */
int find_largest_element(int* arr, int length){
  return 0;
}
int main(){
  int arr[] = {1,2,3,4,5,6};
  printf("largest element in this array is: %d\n",find_largest_element(arr,6));
  
}
