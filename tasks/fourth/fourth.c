#include<stdio.h>
/**
  * James wants a function that adds some factor to the input.
  * His interns put together this code. Unsurprisingly, it didn't work.
  * Fix this code to actually modify input instead of modifying it!
  */
void add_num(int input,int factor){
    input += factor;
}

int main(int argc, char** argv){
    int balance = 100;
    add_num(x,100);
    printf("Now I have %d dollars\n",balance);
}
